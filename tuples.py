thistuple = ("apple", "banana", "cherry", "apple", "cherry")
print(thistuple)

#tuple length
print(len(thistuple))

#creating tuple with one item----> if this comma is not present python won't recognize it as tuple
thistuple = ("apple",)
print(type(thistuple))

# The tuple() Constructor
thistuple = tuple(("apple",))